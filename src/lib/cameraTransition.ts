/**
 * True while the editor camera flies from the orbit view into first person
 * (PlannerCameraSystem). The player leaves the camera alone until it has landed.
 */
export const firstPersonTransition = { active: false };
